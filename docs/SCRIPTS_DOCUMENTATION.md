# ДОКУМЕНТАЦІЯ СКРИПТІВ ПРОЕКТУ SIGOREM

> Детальний опис структури, модулів, функцій та взаємодій у проекті

**Оновлено:** 13 листопада 2025 (повна ревізія всіх модулів)

---

## 📁 СТРУКТУРА ПРОЕКТУ

```
scripts/
├── autoload/              # Глобальні системи (Autoload)
│   ├── BlockRegistry.gd
│   └── ResourceManager.gd
│
├── ui/                    # Користувацький інтерфейс
│   ├── main_menu.gd
│   ├── pause_menu.gd
│   ├── game_hud.gd
│   ├── inventory_ui.gd
│   ├── character_menu.gd
│   ├── crafting_menu.gd
│   ├── settings_menu.gd
│   ├── recipe_button.gd
│   ├── loading_screen_simple.gd
│   └── WorldGenerationSettings.gd
│
└── game/                  # Ігрова логіка
    ├── debug/             # Інструменти для розробки
    │   └── ConsoleCommands.gd
    │
    ├── player/            # Системи гравця
    │   ├── PlayerController.gd
    │   ├── PlayerStats.gd
    │   ├── CameraController.gd
    │   └── BuildController.gd
    │
    ├── systems/           # Ігрові системи
    │   ├── game_events.gd
    │   ├── inventory_system.gd
    │   ├── crafting_system.gd
    │   ├── needs_system.gd
    │   └── status_effects.gd
    │
    └── world/             # Генерація світу
        ├── WorldGenerator.gd
        ├── types.gd
        ├── logger.gd
        ├── ObjectPool.gd
        ├── Quadtree.gd
        ├── voxel_physics.gd
        ├── VoxelNavigation.gd
        └── generation/    # Модулі генерації терейну (19 модулів)
            ├── TerrainGenerator.gd          # Головний менеджер
            ├── ChunkManager.gd              # Система чанків
            ├── ProceduralGeneration.gd      # Базова генерація
            ├── StartingAreaGenerator.gd     # Стартова зона
            ├── OptimizationManager.gd       # Оптимізація
            ├── BestPractices.gd             # Best practices
            ├── PrecomputedPatterns.gd       # Патерни
            ├── SaveLoadManager.gd           # Збереження
            ├── POIGenerator.gd              # Points of Interest
            ├── VegetationManager.gd         # Рослинність
            ├── ThreadingManager.gd          # Багатопоточність
            ├── LODManager.gd                # LOD система
            ├── StructureGenerator.gd        # Структури
            ├── WFCIntegrator.gd             # Wave Function Collapse
            ├── LargeStructureGenerator.gd   # Великі структури
            ├── ErosionSimulator.gd          # Ерозія
            ├── HeightmapLoader.gd           # Heightmap
            ├── SplatMapManager.gd           # Splat mapping
            ├── DetailLayerManager.gd        # Деталі
            └── NativeOptimizer.gd           # Нативна оптимізація
```

---

## 🌐 AUTOLOAD СИСТЕМИ

### BlockRegistry.gd
**Призначення:** Центральний реєстр блоків для воксельного світу  
**Тип:** Autoload (глобальний синглтон)  
**Взаємодія:** WorldGenerator, BuildController, GridMap

**Основні функції:**
- `_load_default_blocks()` - завантаження базових блоків (grass, dirt, stone)
- `_create_simple_block(block_id, color)` - створення блоку з кольором
- `get_mesh_library()` - отримання MeshLibrary для GridMap
- `get_mesh_index(block_id)` - отримання індексу меша по ID блоку

**Сигнали:**
- `block_registered(block_id)` - новий блок зареєстровано
- `blocks_loaded()` - всі блоки завантажено

**Дані:**
- `blocks: Dictionary` - реєстр блоків за ID
- `block_mesh_library: MeshLibrary` - бібліотека мешів
- `id_to_mesh_index: Dictionary` - мапа ID -> mesh_index

**Особливості:** Автоматично створює BoxMesh з кольором для кожного блоку, додає колізії, зберігає у MeshLibrary

---

### ResourceManager.gd
**Призначення:** Менеджер для кешування та завантаження ресурсів  
**Тип:** Autoload (глобальний синглтон)  
**Взаємодія:** UI системи, inventory

**Основні функції:**
- `load_resource(path)` - завантаження ресурсу з кешем
- `preload_resources_by_type(resource_type)` - попереднє завантаження ресурсів
- `get_texture(name)` - отримання текстури за назвою

**Сигнали:**
- `batch_loading_progress(current, total)` - прогрес завантаження
- `batch_loading_complete()` - завантаження завершено

**Дані:**
- `resource_cache: Dictionary` - кеш завантажених ресурсів

**Особливості:** Кешує завантажені ресурси, щоб уникнути повторного завантаження

---

## 🎨 UI СИСТЕМИ

### main_menu.gd
**Призначення:** Головне меню гри  
**Батьківський клас:** Control  
**Взаємодія:** LoadingScreen, SettingsMenu, SceneTree

**Основні функції:**
- `_on_play_button_pressed()` - запуск гри, показує LoadingScreen
- `_on_quit_button_pressed()` - вихід з гри
- `_on_options_button_pressed()` - відкриття налаштувань
- `_show_menu()` - показати меню після закриття налаштувань

**Залежності:**
- `scenes/ui/settings_menu.tscn`
- `scenes/world.tscn`

**Особливості:** Завантажує LoadingScreen перед переходом до світу

---

### pause_menu.gd
**Призначення:** Меню паузи в грі  
**Батьківський клас:** Control  
**Взаємодія:** SettingsMenu, SceneTree

**Основні функції:**
- `_on_continue_pressed()` - продовжити гру (unpause)
- `_on_quit_pressed()` - вихід з гри
- `_on_settings_pressed()` - відкриття налаштувань у паузі
- `_show_buttons() / _hide_buttons()` - керування видимістю кнопок

**Особливості:**
- Працює в режимі `PROCESS_MODE_WHEN_PAUSED`
- Відкривається на ESC (ui_cancel)
- Зупиняє дерево сцени через `get_tree().paused = true`

---

### game_hud.gd
**Призначення:** HUD гри з інформацією та debug панеллю  
**Батьківський клас:** CanvasLayer  
**Взаємодія:** Player, BuildMode, WorldGenerationSettings, TerrainGenerator

**Основні функції:**
- `toggle_world_gen_settings()` - перемикання меню генерації світу (F10)
- `update_terrain_debug_info()` - оновлення debug інформації
- `_process(delta)` - оновлення стану build mode та debug панелі

**Дані що відображаються:**
- Підказки керування (WASD, build mode, тощо)
- Build Mode статус
- Debug HUD: seed, chunk size, активні чанки, позиція гравця

**Особливості:** Debug HUD можна перемкнути через `toggle_debug_hud` action

---

### inventory_ui.gd
**Призначення:** UI для відображення інвентаря  
**Батьківський клас:** Control  
**Взаємодія:** InventorySystem (Autoload)

**Основні функції:**
- `update_ui()` - оновлення слотів інвентаря
- `_unhandled_input(event)` - перемикання видимості (I)

**Сигнали підписки:**
- `InventorySystem.inventory_changed` - оновити UI при зміні

**Особливості:**
- Створює 48 слотів (MAX_SLOTS)
- Динамічно оновлює іконки та кількість предметів
- Показує/ховає на клавішу I

---

### character_menu.gd
**Призначення:** Меню персонажа зі статистиками (HP, stamina, hunger)  
**Батьківський клас:** Control  
**Взаємодія:** PlayerStats, NeedsSystem

**Основні функції:**
- `_connect_to_player()` - підключення до систем гравця
- `on_stat_changed(stat_name, value, max_value)` - оновлення статистик
- `on_needs_changed(need_name, value, max_value)` - оновлення потреб

**Сигнали підписки:**
- `PlayerStats.stat_changed` - зміна здоров'я/витривалості
- `NeedsSystem.needs_changed` - зміна голоду/спраги

**Особливості:** Відкривається на клавішу C (character_menu)

---

### crafting_menu.gd
**Призначення:** Меню крафтингу з доступними рецептами  
**Батьківський клас:** Control  
**Взаємодія:** CraftingSystem, InventorySystem (Autoload)

**Основні функції:**
- `update_recipe_list()` - оновлення списку доступних рецептів
- `on_recipe_selected(recipe)` - крафт обраного рецепту

**Сигнали підписки:**
- `InventorySystem.inventory_changed` - оновити список рецептів

**Особливості:**
- Показує тільки рецепти які можна зробити зараз
- Використовує `recipe_button.tscn` для кнопок
- Відкривається на клавішу K (crafting_menu)

---

### settings_menu.gd
**Призначення:** Меню налаштувань (роздільна здатність, повний екран)  
**Батьківський клас:** Control  
**Взаємодія:** DisplayServer

**Основні функції:**
- `_populate_resolution_list()` - заповнення списку роздільностей
- `_on_resolution_selected(index)` - зміна роздільності
- `_on_fullscreen_toggled(pressed)` - перемикання повноекранного режиму
- `_apply_selected_resolution()` - застосування роздільності

**Дані:**
- `RESOLUTIONS: Array` - список підтриманих роздільностей (від 640x480 до 3840x2160)

**Особливості:** Підтримує custom роздільність, показує поточну роздільність як "(current)"

---

### WorldGenerationSettings.gd
**Призначення:** UI для налаштувань генерації світу  
**Батьківський клас:** Control  
**Взаємодія:** TerrainGenerator, всі модулі генерації

**Основні функції:**
- `load_current_settings()` - завантаження налаштувань з TerrainGenerator
- `apply_settings()` - застосування налаштувань до TerrainGenerator
- `generate_world()` - регенерація світу з новими налаштуваннями
- `_on_randomize_seed_pressed()` - рандомізація seed
- `_on_auto_update_toggled(pressed)` - автоматичне оновлення при зміні

**Налаштування модулів:**
- **Core Modules:** ProceduralGeneration, Chunking, Optimization, BestPractices
- **Параметри:** chunk_size, chunk_radius, height_amplitude, base_height, max_height
- **Noise:** seed, frequency, noise_type, fractal_type, octaves
- **Advanced:** Structures, LOD, Threading, Vegetation, Heightmap, SplatMapping, DetailLayers

**Особливості:**
- Відкривається на F10 (через GameHUD)
- Може автоматично застосовувати зміни (auto_update)
- Підтримує вкладки: Основні, Розширені, Оптимізація
- Телепортує гравця на стартову зону після регенерації

---

### recipe_button.gd
**Призначення:** Кнопка з інформацією про рецепт  
**Батьківський клас:** Button  
**Взаємодія:** CraftingMenu

**Основні функції:**
- `set_recipe(new_recipe)` - встановлення даних рецепту

**Дані:**
- `recipe: CraftingRecipe` - дані рецепту

**Особливості:** Відображає іконку та назву з кількістю результату

---

### loading_screen_simple.gd
**Призначення:** Екран завантаження з прогресом  
**Батьківський клас:** CanvasLayer  
**Взаємодія:** MainMenu, SceneTree

**Основні функції:**
- `start_loading()` - запуск завантаження з анімацією прогресу
- `_update_progress()` - оновлення прогресу
- `_on_loading_complete()` - завершення завантаження та перехід до world

**Етапи завантаження:**
1. Loading blocks
2. Loading textures
3. Initializing world

**Особливості:** Використовує таймери для імітації завантаження (по 0.5 сек на етап)

---

## 🎮 PLAYER СИСТЕМИ

### PlayerController.gd
**Призначення:** Керування рухом та поведінкою гравця  
**Батьківський клас:** CharacterBody3D  
**Взаємодія:** Camera, BuildMode, GridMap

**Основні функції:**
- `_handle_normal_movement(delta)` - звичайний рух з гравітацією
- `_handle_flight_movement(delta)` - польотний режим (для дебагу)
- `_get_direction_relative_to_camera(input_vec)` - перетворення input відносно камери
- `_ensure_movement_actions()` - створення input actions якщо їх немає

**Налаштування:**
- `speed: float` - швидкість руху (5.0)
- `acceleration: float` - прискорення (12.0)
- `jump_velocity: float` - сила стрибка (6.0)
- `flight_mode: bool` - польотний режим (F)
- `speed_multiplier: float` - множник швидкості (PageUp/PageDown)

**Input Actions:**
- move_left (A), move_right (D), move_forward (W), move_back (S)
- jump (Space)
- F - перемикання польотного режиму
- PageUp/PageDown - зміна швидкості

**Особливості:**
- Підтримує 8-напрямковий рух відносно камери
- Польотний режим для огляду світу
- Налаштовувана швидкість (0.1x - 10.0x)

---

### PlayerStats.gd
**Призначення:** Система статистик гравця (здоров'я, витривалість)  
**Батьківський клас:** Node  
**Взаємодія:** CharacterMenu, NeedsSystem, StatusEffects

**Основні функції:**
- `take_damage(amount)` - отримання пошкодження
- `heal(amount)` - відновлення здоров'я
- `use_stamina(amount)` - використання витривалості
- `regenerate_stamina(amount)` - відновлення витривалості

**Сигнали:**
- `stat_changed(stat_name, value, max_value)` - зміна статистики
- `health_depleted` - здоров'я вичерпано

**Дані:**
- `health / max_health: float` - здоров'я (100/100)
- `stamina / max_stamina: float` - витривалість (100/100)
- `stamina_regen_rate: float` - швидкість регенерації (5.0)
- `stamina_regen_modifier: float` - модифікатор регенерації (1.0)

**Особливості:** Автоматична регенерація витривалості у `_process()`

---

### CameraController.gd
**Призначення:** 2.5D діорамна камера з слідуванням за гравцем  
**Батьківський клас:** Camera3D  
**Взаємодія:** Player

**Основні функції:**
- `_physics_process(delta)` - плавне слідування за гравцем
- `_unhandled_input(event)` - зум та обертання камери

**Налаштування:**
- `target: Node3D` - ціль слідування (Player)
- `offset: Vector3` - зміщення камери (10, 15, 10)
- `smooth_speed: float` - швидкість слідування (5.0)
- `diorama_angle: float` - кут нахилу (45°)
- `min_zoom / max_zoom: float` - межі зуму (5.0 / 30.0)
- `rotation_enabled: bool` - можливість обертання (Q/E)

**Input:**
- Mouse Wheel - зум камери
- Q - обертання ліворуч (на 45°)
- E - обертання праворуч (на 45°)

**Особливості:**
- Діорамний вигляд як у Octopath Traveler
- 8 напрямків обертання (кожні 45°)
- Плавне слідування через lerp

---

### BuildController.gd
**Призначення:** Система будівництва блоків  
**Батьківський клас:** Node  
**Взаємодія:** GridMap, BlockRegistry, Camera, VoxelPhysics

**Основні функції:**
- `_unhandled_input(event)` - обробка build mode та розміщення/видалення блоків
- `_physics_process(delta)` - оновлення позиції ghost block
- `get_grid_map() / get_camera()` - отримання посилань на GridMap та камеру

**Дані:**
- `build_mode: bool` - чи активний режим будівництва
- `selected_block_id: int` - ID обраного блоку
- `ghost_mesh: MeshInstance3D` - напівпрозорий блок-примара

**Input:**
- B - перемикання build mode
- ЛКМ - поставити блок (на поверхню + normal)
- ПКМ - видалити блок (з поверхні - normal)

**Особливості:**
- Ghost block показує де буде розміщено блок
- Використовує raycast для визначення позиції
- Перевіряє чи блок вже існує перед розміщенням

---

## 🔧 ІГРОВІ СИСТЕМИ

### game_events.gd
**Призначення:** Глобальна система подій для комунікації між модулями  
**Тип:** Autoload (глобальний синглтон)  
**Взаємодія:** всі ігрові системи

**Сигнали:**
- `item_picked_up(item, count)` - предмет підібрано
- `item_used(item, source)` - предмет використано
- `item_crafted(recipe)` - предмет створено
- `block_placed(position, block_type)` - блок розміщено
- `block_destroyed(position)` - блок знищено

**Особливості:** Дозволяє системам комунікувати без прямих залежностей (decoupling)

---

### inventory_system.gd
**Призначення:** Система інвентаря з підтримкою addon rubonnek.inventory_manager  
**Тип:** Autoload (глобальний синглтон)  
**Взаємодія:** CraftingSystem, InventoryUI

**Основні функції:**
- `add_item(item, quantity)` - додати предмет
- `remove_item(item_id, quantity)` - видалити предмет
- `count_item(item_id)` - порахувати кількість предмета
- `swap_slots(first_index, second_index)` - обмін слотів
- `transfer(from_index, to_index, amount)` - переміщення предметів

**Сигнали:**
- `inventory_changed` - інвентар змінено

**Дані:**
- `MAX_SLOTS: int` - максимум слотів (48)
- `slots: Array[ItemSlot]` - масив слотів

**Клас ItemSlot:**
- `item: ItemResource` - предмет
- `quantity: int` - кількість

**Особливості:**
- Використовує addon для керування стеками
- Автоматична реєстрація предметів з `resources/items/`
- Конвертація string ID в numeric ID для оптимізації

---

### crafting_system.gd
**Призначення:** Система крафтингу предметів  
**Тип:** Autoload (глобальний синглтон)  
**Взаємодія:** InventorySystem, CraftingMenu

**Основні функції:**
- `load_recipes()` - завантаження рецептів з `resources/recipes/`
- `get_available_recipes(inventory_system)` - отримання доступних рецептів
- `can_craft(recipe, inventory_system)` - перевірка можливості крафту
- `craft_item(recipe, inventory_system)` - створення предмета

**Дані:**
- `recipes: Array[CraftingRecipe]` - список рецептів

**Особливості:**
- Автоматично сканує папку рецептів при запуску
- Перевіряє інвентар перед крафтом
- Емітує `GameEvents.item_crafted` після успішного крафту

---

### needs_system.gd
**Призначення:** Система потреб гравця (голод, спрага, сон)  
**Батьківський клас:** Node  
**Взаємодія:** CharacterMenu, StatusEffects

**Основні функції:**
- `_process(delta)` - поступове зменшення потреб

**Сигнали:**
- `needs_changed(need_name, value, max_value)` - потреба змінилась

**Дані:**
- `hunger / max_hunger: float` - голод (100/100), drain_rate: 0.1
- `thirst / max_thirst: float` - спрага (100/100), drain_rate: 0.15
- `sleepiness / max_sleepiness: float` - сон (100/100), drain_rate: 0.05

**Особливості:** Автоматичне зменшення всіх потреб у `_process()`

---

### status_effects.gd
**Призначення:** Система статус-ефектів залежно від потреб  
**Батьківський клас:** Node  
**Взаємодія:** NeedsSystem, PlayerStats

**Основні функції:**
- `check_states(need_name, value, max_value)` - перевірка станів
- `apply_state_effects()` - застосування ефектів

**Enum PlayerState:**
- NORMAL, HUNGRY, STARVING, THIRSTY, DEHYDRATED, TIRED, EXHAUSTED, COLD, HOT, SICK, INJURED

**Дані:**
- `active_states: Array[PlayerState]` - активні стани

**Логіка ефектів:**
- `hunger < 20` → STARVING (пошкодження 0.1/tick)
- `hunger < 50` → HUNGRY
- `thirst < 20` → DEHYDRATED (пошкодження 0.1/tick)
- `thirst < 50` → THIRSTY
- `sleepiness < 20` → EXHAUSTED (stamina_regen_modifier = 0.5)
- `sleepiness < 50` → TIRED

**Особливості:** Автоматично підписується на `NeedsSystem.needs_changed`

---

## 🌍 WORLD СИСТЕМИ

### WorldGenerator.gd
**Призначення:** Обгортка для TerrainGenerator, забезпечує сумісність  
**Батьківський клас:** GridMap (@tool)  
**Взаємодія:** TerrainGenerator, BlockRegistry

**Основні функції:**
- `_setup_mesh_library()` - встановлення MeshLibrary з BlockRegistry
- `_setup_mesh_library_editor()` - створення MeshLibrary для редактора
- `_setup_terrain_generator()` - налаштування TerrainGenerator

**Особливості:**
- Працює в редакторі (@tool)
- Автоматично створює базові блоки для редактора
- Делегує генерацію до TerrainGenerator

---

### types.gd
**Призначення:** Типи, константи та утиліти для генерації світу  

**Константи:**
- `DEFAULT_CHUNK_SIZE: Vector2i(50, 50)`
- `DEFAULT_CHUNK_RADIUS: int = 5`
- `DEFAULT_HEIGHT_AMPLITUDE: int = 5`
- `DEFAULT_BASE_HEIGHT: int = 5`

**Enum BlockType:**
- AIR (-1), GRASS (0), DIRT (1), STONE (2), SAND (3), WATER (4), WOOD_LOG (5), LEAVES (6)

**Enum BiomeType:**
- PLAINS, FOREST, DESERT, MOUNTAINS, TUNDRA, SWAMP

**Класи:**
- `ChunkData` - дані чанка (position, blocks, is_generated, last_accessed)
- `WorldMetadata` - метадані світу (seed, created_at, world_name, chunk_size, version)
- `CoordinateUtils` - утиліти для конвертації координат (world_to_chunk, chunk_to_world)
- `GenerationConfig` - конфігурація генерації (use_procedural, noise_seed, chunk_size, тощо)

**Особливості:** Централізований набір типів для всієї системи генерації

---

### logger.gd (WorldLogger)
**Призначення:** Система логування з категоріями для генерації світу  
**Батьківський клас:** Node  

**Enum LogLevel:**
- ERROR (0), WARNING (1), INFO (2), DEBUG (3), VERBOSE (4)

**Enum LogCategory:**
- GENERAL, GENERATION, CHUNKING, PERFORMANCE, SAVE_LOAD, STRUCTURES, VEGETATION

**Основні функції:**
- `log_error/warning/info/debug/verbose(message, category)` - логування з рівнем
- `enable_category(category) / disable_category(category)` - фільтрація категорій
- `generation/chunking/performance(message, level)` - зручні методи для категорій

**Налаштування:**
- `current_level: LogLevel` - мінімальний рівень логування
- `enabled_categories: Array[LogCategory]` - фільтр категорій
- `save_to_file: bool` - збереження в файл
- `log_file_path: String` - шлях до файлу ("user://world_generation.log")

**Особливості:**
- Фільтрація по рівню та категорії
- Буферизація для файлу (записує кожні 10 повідомлень)
- Форматований вивід з timestamp та категорією

---

### ObjectPool.gd
**Призначення:** Memory pooling - повторне використання об'єктів  
**Батьківський клас:** Node  

**Основні функції:**
- `get_object(scene_path, parent)` - отримати об'єкт з пулу або створити
- `return_object(obj)` - повернути об'єкт в пул
- `preload_objects(scene_path, count)` - попереднє створення об'єктів
- `get_pool_stats(scene_path)` - статистика пулу

**Спеціалізовані методи:**
- `get_particle_effect(effect_path, parent)` - ефекти частинок
- `get_projectile(projectile_path, parent)` - снаряди
- `get_enemy(enemy_path, parent)` - вороги

**Налаштування:**
- `max_pool_size: int` - максимум об'єктів в пулі (100)
- `preload_count: int` - кількість попередньо створених об'єктів (10)

**Статистика пулу:**
- `created: int` - кількість створених об'єктів
- `reused: int` - кількість повторно використаних
- `active: int` - кількість активних об'єктів

**Особливості:**
- Оптимізує створення часто використовуваних об'єктів
- Автоматичний reset об'єктів при отриманні з пулу
- Деактивація об'єктів при поверненні в пул

---

### Quadtree.gd
**Призначення:** Spatial partitioning для швидкого пошуку чанків  
**Батьківський клас:** Node  

**Клас QuadtreeNode:**
- `bounds: Rect2i` - межі вузла
- `capacity: int` - максимум точок (4)
- `points: Array[Vector2i]` - чанки в цьому вузлі
- `divided: bool` - чи розділено на дочірні вузли

**Основні функції:**
- `insert_chunk(chunk_pos)` - додати чанк
- `get_chunks_in_radius(center, radius)` - чанки в радіусі
- `get_chunks_in_rect(rect)` - чанки в прямокутнику
- `find_nearest_chunk(point)` - найближчий чанк
- `get_stats()` - статистика (total_chunks, max_depth, node_count)

**Спеціалізовані запити:**
- `get_chunks_in_view_frustum(camera_pos, forward, distance)` - чанки в полі зору

**Налаштування:**
- `world_bounds: Rect2i` - межі світу (-1000, -1000, 2000, 2000)
- `node_capacity: int` - максимум точок на вузол (4)

**Особливості:**
- Рекурсивний поділ на 4 дочірні вузли
- Оптимізує перевірку відстаней (замість O(n) -> O(log n))
- Може обробляти мільйони чанків ефективно

---

### voxel_physics.gd
**Призначення:** Воксельна фізика обвалів як у Lay of Land  
**Батьківський клас:** Node  
**Взаємодія:** GridMap, Player

**Основні функції:**
- `check_stability_around(position)` - перевірка стабільності навколо блоку
- `is_block_supported(position)` - чи блок підтриманий
- `start_collapse(position)` - запуск обвалу блоку
- `create_falling_block(position, block_id)` - створення падаючого блоку (RigidBody3D)
- `excavate_area(center, radius)` - викопування області з ланцюговою реакцією

**Сигнали:**
- `block_collapsed(position)` - блок обвалився
- `collapse_chain_started(origin)` - почалась ланцюгова реакція

**Налаштування:**
- `gravity_strength: float` - сила гравітації (9.8)
- `collapse_delay: float` - затримка обвалу (0.1)
- `support_radius: int` - радіус перевірки підтримки (1)
- `max_unsupported_distance: int` - максимальна відстань без підтримки (3)

**Оптимізація фізики:**
- `enable_chunk_physics_optimization: bool` - оптимізація по чанках
- `max_physics_updates_per_frame: int` - максимум оновлень за кадр (20)
- `physics_update_distance_limit: float` - ліміт відстані від гравця (50.0)

**Дані:**
- `falling_blocks: Dictionary` - падаючі блоки (position -> RigidBody3D)
- `block_support_map: Dictionary` - карта підтримки блоків

**Особливості:**
- Реалістичні обвали з діагональною підтримкою
- Ланцюгові руйнування при видаленні основи
- Падаючі блоки з rotation та impulse
- Оптимізація - фізика тільки поблизу гравця

---

### VoxelNavigation.gd
**Призначення:** Pathfinding через воксельний світ  
**Батьківський клас:** Node  

**Основні функції:**
- `find_path(start_pos, end_pos)` - знайти шлях між точками
- `find_path_godot_navigation(start, end)` - через Godot NavigationServer3D
- `find_path_voxel_navigation(start, end)` - через власну A* реалізацію
- `a_star_search(start, end)` - A* алгоритм для воксельних світів
- `is_walkable(pos)` - перевірка прохідності позиції
- `is_line_of_sight_clear(start, end)` - перевірка видимості

**Налаштування:**
- `enable_navigation: bool` - чи увімкнена навігація (false - заготовка)
- `use_godot_navigation_server: bool` - використовувати Godot NavigationServer (true)
- `use_voxel_pathfinding: bool` - власна voxel навігація (false)
- `max_path_length: int` - максимальна довжина шляху (1000)
- `agent_height / agent_radius: float` - параметри агента (2.0 / 0.5)

**Voxel Pathfinding:**
- `max_jump_height: int` - максимальна висота стрибка (2)
- `max_drop_height: int` - максимальна висота падіння (5)

**Особливості:**
- Підтримує Godot NavigationServer3D або власну A*
- Спрощена A* реалізація для воксельних світів (6 напрямків)
- Заготовка для майбутнього AI та pathfinding

---

## 🏔️ WORLD GENERATION СИСТЕМИ

### TerrainGenerator.gd
**Призначення:** Головний менеджер генерації терейну, модульна архітектура  
**Батьківський клас:** Node (@tool)  
**Взаємодія:** всі модулі generation/

**Модулі:**
- `procedural_module: ProceduralGeneration` - базова процедурна генерація
- `chunk_module: ChunkManager` - система чанків
- `starting_area_module: StartingAreaGenerator` - стартова зона
- `structure_module: StructureGenerator` - структури (WFC)
- `lod_module: LODManager` - система LOD
- `threading_module: ThreadingManager` - багатопоточність
- `vegetation_module: VegetationManager` - рослинність
- `heightmap_module: HeightmapLoader` - завантаження heightmap
- `splat_module: SplatMapManager` - splat mapping
- `detail_module: DetailLayerManager` - деталізація
- `optimization_module: OptimizationManager` - оптимізація
- `save_load_module: SaveLoadManager` - збереження/завантаження
- `native_optimizer: NativeOptimizer` - нативна оптимізація
- `pattern_manager: PrecomputedPatterns` - попередньо обчислені патерни
- `best_practices: BestPractices` - best practices

**Основні функції:**
- `initialize_modules()` - ініціалізація всіх модулів
- `generate_initial_terrain()` - початкова генерація світу
- `regenerate_terrain()` - регенерація світу
- `teleport_player_to_starting_area()` - телепорт гравця на стартову зону
- `cleanup_modules()` - очищення модулів

**Налаштування модулів (export):**
- `use_procedural_generation: bool` (true)
- `use_chunking: bool` (true)
- `use_starting_area: bool` (true)
- `use_structures: bool` (false - заготовка)
- `use_lod: bool` (false - заготовка)
- `use_threading: bool` (false - заготовка)
- `use_vegetation: bool` (false - заготовка)
- `use_heightmap: bool` (false - заготовка)
- `use_optimization: bool` (false - гальмує)
- `use_best_practices: bool` (true)

**Параметри генерації:**
- `chunk_size: Vector2i` (32x32)
- `chunk_radius: int` (3)
- `height_amplitude: int` (32)
- `base_height: int` (16)
- `max_height: int` (128)

**Особливості:**
- Модульна архітектура - можна вмикати/вимикати модулі
- Працює в редакторі (@tool)
- Автоматично знаходить гравця якщо не встановлений
- Додається до групи "terrain_generator"

---

### ChunkManager.gd
**Призначення:** Управління чанками - завантаження, вивантаження, culling  
**Батьківський клас:** Node  

**Основні функції:**
- `generate_initial_chunks(gridmap)` - генерація початкових чанків
- `update_chunks(gridmap)` - оновлення при русі гравця
- `generate_chunk(gridmap, chunk_pos)` - генерація окремого чанка
- `remove_chunk(gridmap, chunk_pos)` - видалення чанка
- `cull_distant_chunks(gridmap)` - видалення далеких чанків
- `is_chunk_in_frustum(chunk_pos, camera)` - перевірка чи чанк у полі зору

**Partial Mesh Updates:**
- `register_block_change(position, old_mesh, new_mesh)` - реєстрація зміни блоку
- `process_partial_updates(delta)` - обробка змінених блоків
- `update_single_block_mesh(gridmap, position)` - оновлення одного блоку

**Preloading Buffer:**
- `update_preload_queue()` - оновлення черги попереднього завантаження
- `process_preload_queue()` - обробка черги

**Налаштування:**
- `chunk_size: Vector2i` - розмір чанка (50x50)
- `chunk_radius: int` - радіус генерації (5)
- `enable_culling: bool` - видалення далеких чанків (true)
- `max_distance: float` - максимальна відстань (100.0)
- `enable_frustum_culling: bool` - culling по frustum (true)
- `min_cull_interval: float` - мінімальний інтервал видалення (0.5 сек)
- `max_chunks_to_remove_per_frame: int` - максимум чанків для видалення (3)

**Preloading налаштування:**
- `enable_preloading: bool` - попереднє завантаження (true)
- `preload_radius: int` - радіус preload (3)
- `max_preload_per_frame: int` - максимум preload за кадр (2)

**Дані:**
- `active_chunks: Dictionary` - активні чанки (Vector2i -> bool)
- `current_player_chunk: Vector2i` - поточний чанк гравця
- `preload_queue: Array[Vector2i]` - черга preload
- `modified_blocks: Dictionary` - змінені блоки

**Особливості:**
- Захист від занадто частого culling (min_cull_interval)
- Обмеження видалення чанків за кадр (max_chunks_to_remove_per_frame)
- Попереднє завантаження чанків (preloading buffer)
- Partial mesh updates (не повністю працює)
- Frustum culling (спрощена версія)

---

### ProceduralGeneration.gd
**Призначення:** Базова процедурна генерація терейну з шуму  
**Батьківський клас:** Node  

**Основні функції:**
- `generate_terrain(gridmap, start_pos, size)` - генерація області
- `generate_chunk(gridmap, chunk_pos, optimization)` - генерація чанка
- `get_height_at(x, z)` - отримати висоту в точці
- `sample_2dv(point)` - семплінг шуму з додатковими шарами
- `generate_chunk_with_boundaries(gridmap, chunk_pos, chunk_size, chunk_start)` - з врахуванням границь

**Biome Transitions:**
- `get_biome_at_position(x, z)` - визначення біома з blending
- `_calculate_biome_weights(x, z)` - обчислення ваг біомів
- `_blend_biomes(biomes, weights)` - інтерполяція між біомами

**Cave Generation:**
- `generate_caves_in_chunk(gridmap, chunk_pos, chunk_size)` - генерація печер (вимкнено)

**Boundary Fix:**
- `_gather_neighbor_chunk_data(gridmap, chunk_pos, chunk_size)` - збір даних сусідів
- `_get_height_with_boundary_smoothing(x, z, neighbor_heights)` - згладжування границь

**Налаштування:**
- `noise: FastNoiseLite` - генератор шуму
- `height_amplitude: int` - амплітуда висоти (5)
- `base_height: int` - базова висота (5)
- `max_height: int` - максимальна висота (64)

**Biome налаштування:**
- `enable_biomes: bool` - біоми (true)
- `biome_blend_distance: float` - відстань blending (3.0)
- `biome_scale: float` - масштаб біомів (100.0)

**Cave налаштування:**
- `enable_caves: bool` - печери (false - заготовка)
- `cave_density: float` - щільність печер (0.3)
- `cave_noise_scale: float` - масштаб шуму печер (0.05)

**Додаткові шари:**
- `extra_terrain_noise_layers: Array[FastNoiseLite]` - додаткові шари шуму
- `terrain_height_multiplier: float` (150.0)
- `terrain_height_offset: float` (0.0)

**Особливості:**
- Підтримка біомів з плавним blending
- Boundary smoothing для уникнення стиків чанків (частково працює)
- Cave generation (заготовка, вимкнено)
- Додаткові шари шуму для складнішого рельєфу

---

### StartingAreaGenerator.gd
**Призначення:** Генерація безпечної стартової зони для гравця  
**Батьківський клас:** Node  

**Основні функції:**
- `generate_starting_area(gridmap, center_chunk, chunk_size)` - створення стартової зони
- `get_safe_spawn_position(chunk_size)` - отримання позиції спавну
- `find_safe_spawn_with_collision_check(gridmap, chunk_size, player_height)` - пошук безпечної позиції з перевіркою колізій
- `_is_position_safe(gridmap, pos, player_height)` - перевірка безпеки позиції

**Налаштування:**
- `enable_starting_area: bool` - чи увімкнена стартова зона (true)
- `starting_area_size: int` - розмір зони в блоках (5x5)
- `flatten_starting_area: bool` - зробити зону плоскою (true)
- `starting_area_height: int` - висота зони (16, синхронізовано з base_height)
- `always_spawn_on_starting_area: bool` - завжди спавнити гравця тут (true)

**Функції (заготовки):**
- `clear_trees_in_area: bool` - очистити дерева (true)
- `add_tutorial_structures: bool` - додати навчальні структури (false)
- `create_paths: bool` - створити доріжки (false)
- `add_basic_resources: bool` - додати базові ресурси (false)

**Дані:**
- `starting_area_chunks: Array` - список чанків стартової зони

**Особливості:**
- Створює плоску зону 5x5 блоків
- Очищає всі блоки вище starting_area_height
- Перевіряє колізії для безпечного спавну (підлога, тіло, голова)
- Автоматично телепортує гравця після регенерації світу

---

### POIGenerator.gd
**Призначення:** Генерація Points of Interest (цікаві локації)  
**Батьківський клас:** Node  

**Основні функції:**
- `generate_pois_for_chunk(gridmap, chunk_pos, chunk_size)` - генерація POI для чанка
- `determine_poi_type(chunk_pos)` - визначення типу POI
- `is_too_close_to_existing_poi(chunk_pos)` - перевірка відстані між POI
- `generate_cave_poi(gridmap, chunk_pos, chunk_size)` - генерація печер
- `generate_ruins_poi()` - генерація руїн (заготовка)
- `generate_shrine_poi()` - генерація святилищ (заготовка)

**Налаштування:**
- `enable_poi_generation: bool` - увімкнено (false - заготовка)
- `poi_density: float` - щільність POI (0.05)
- `min_distance_between_pois: int` - мінімальна відстань (5 чанків)

**Типи POI:**
- `generate_caves: bool` - печери (true)
- `generate_ruins: bool` - руїни (false - заготовка)
- `generate_shrines: bool` - святилища (false - заготовка)
- `generate_treasure_sites: bool` - скарби (false - заготовка)
- `generate_special_biomes: bool` - особливі біоми (false - заготовка)

**Cave налаштування:**
- `cave_min_size: int` - мінімальний розмір (10)
- `cave_max_size: int` - максимальний розмір (50)
- `cave_entrance_probability: float` - ймовірність входу (0.3)

**Дані:**
- `generated_pois: Dictionary` - згенеровані POI (Vector2i -> {"type", "data"})
- `poi_noise: FastNoiseLite` - шум для POI (seed: 99999)

**Особливості:**
- Детермінована генерація через фіксований seed
- Контроль мінімальної відстані між POI
- Різні типи POI (печери, руїни, святилища, скарби)

---

### PrecomputedPatterns.gd
**Призначення:** Попередньо обчислені патерни для оптимізації  
**Батьківський клас:** Node  

**Основні функції:**
- `precompute_terrain_patterns()` - обчислення патернів місцевості
- `precompute_structure_patterns()` - обчислення патернів структур
- `precompute_biome_patterns()` - обчислення патернів біомів
- `create_terrain_pattern(seed_offset)` - створення одного патерну
- `get_pattern(index)` - отримання патерну за індексом
- `save_patterns_to_file()` / `load_patterns_from_file()` - збереження/завантаження

**Налаштування:**
- `pattern_resolution: int` - роздільність патерну (256)
- `num_patterns: int` - кількість патернів (10)
- `pattern_scale: float` - масштаб патерну (0.1)

**Дані патерну:**
- `height_map: Array` - карта висот
- `steepness_map: Array` - карта крутості
- `biome_map: Array` - карта біомів
- `resolution: int` - роздільність
- `seed: int` - seed патерну

**Типи патернів:**
- `terrain_patterns: Array` - патерни місцевості
- `structure_patterns: Array` - патерни структур (будинки, вежі)
- `biome_patterns: Array` - патерни біомів (ліс, пустеля, гори, рівнини, болото)

**Особливості:**
- Зменшує CPU навантаження під час runtime
- Патерни обчислюються при запуску гри
- Можна зберігати/завантажувати з файлу
- Підтримка різних типів патернів (місцевість, структури, біоми)

---

### SaveLoadManager.gd
**Призначення:** Збереження та завантаження терейну  
**Батьківський клас:** Node  

**Основні функції:**
- `save_chunk_data(chunk_pos, chunk_data)` - збереження чанка
- `load_chunk_data(chunk_pos)` - завантаження чанка
- `save_world_metadata(metadata)` - збереження метаданих світу
- `load_world_metadata()` - завантаження метаданих
- `auto_save()` - автозбереження
- `get_all_saved_chunks()` - список збережених чанків

**Компресія:**
- `compress_string(data)` - стиснення даних
- `decompress_string(data)` - розпакування даних

**Налаштування:**
- `save_directory: String` - директорія збереження ("user://terrain_data/")
- `auto_save_interval: float` - інтервал автозбереження (300 сек)
- `compress_data: bool` - стиснення даних (true)

**Формат збереження:**
```gdscript
{
	"chunk_pos": [x, y],
	"timestamp": unix_time,
	"data": chunk_data
}
```

**Файлова структура:**
- `user://terrain_data/chunk_{x}_{z}.json` - файли чанків
- `user://terrain_data/world_metadata.json` - метадані світу

**Особливості:**
- Автозбереження кожні 5 хвилин
- Компресія даних для економії місця
- JSON формат для читабельності
- Автоматичне створення директорії

---

### VegetationManager.gd
**Призначення:** Управління рослинністю через MultiMesh  
**Батьківський клас:** Node  

**Основні функції:**
- `generate_multimesh_for_chunk(chunk_pos, gridmap)` - генерація рослин для чанка
- `generate_multimesh_positions(chunk_pos, gridmap)` - обчислення позицій рослин
- `remove_multimesh_for_chunk(chunk_pos)` - видалення рослин для чанка
- `update_multimesh_visibility(camera_pos)` - оновлення видимості (LOD)

**Налаштування:**
- `multimesh_cast_shadow: ShadowCastingSetting` - тінь від рослин
- `multimesh_radius: int` - радіус генерації (6)
- `multimesh_coverage: float` - покриття (0.0-1.0, за замовч. 0.5)
- `multimesh_jitter: float` - випадкове зміщення (0.0-10.0, за замовч. 5.0)
- `multimesh_on_cliffs: bool` - генерувати на схилах (false)
- `multimesh_steep_threshold: float` - поріг крутості (0.5)
- `multimesh_repeats: int` - кількість повторень (1-10)

**Дані:**
- `multimesh_dict: Dictionary` - MultiMesh для кожного чанка (chunk_pos -> MultiMeshInstance3D)
- `multimesh_mesh: Mesh` - меш для рослин (за замовч. RibbonTrailMesh)
- `multimesh_noise: FastNoiseLite` - шум для розподілу рослин

**Особливості:**
- Batching через MultiMesh (тисячі екземплярів без втрати продуктивності)
- Випадковий розподіл через шум
- Підтримка тіней
- Налаштування щільності та розміру
- Фільтрація по крутості (не генерувати на схилах)

---

### ThreadingManager.gd
**Призначення:** Багатопоточна генерація терейну  
**Батьківський клас:** Node  

**Основні функції:**
- `start_generation()` - запуск потоків
- `stop_generation()` - зупинка потоків
- `queue_generation_task(task)` - додати завдання до черги
- `_generation_worker()` - worker функція для потоків
- `get_completed_tasks()` - отримати завершені завдання

**Налаштування:**
- `max_threads: int` - максимум потоків (4)

**Дані:**
- `thread_pool: Array[Thread]` - пул потоків
- `generation_queue: Array` - черга завдань
- `semaphore: Semaphore` - синхронізація потоків
- `mutex: Mutex` - захист спільних даних
- `running: bool` - чи працюють потоки

**Формат завдання:**
```gdscript
{
	"type": "generate_chunk",
	"chunk_pos": Vector2i(x, z),
	"params": {...}
}
```

**Особливості:**
- Пул потоків для ефективного використання CPU
- Черга завдань для організації роботи
- Semaphore та Mutex для безпечної багатопоточності
- Асинхронна генерація без блокування основного потоку
- Заготовка (вимкнено, не повністю інтегровано)

---

### LODManager.gd
**Призначення:** Level of Detail система для оптимізації рендерингу  
**Батьківський клас:** Node  
**Статус:** ⚠️ Заготовка (вимкнено)

**Основні функції:**
- `get_lod_level(chunk_pos, player_pos)` - визначення LOD рівня для чанка
- `apply_lod_to_chunk(chunk_pos, lod_level)` - застосування LOD
- `update_chunk_lod(camera_pos)` - оновлення LOD для всіх чанків

**Налаштування:**
- `lod_distances: Array[float]` - відстані для LOD рівнів
- `lod_resolutions: Array[float]` - роздільність для кожного LOD

**Особливості:**
- Динамічна деталізація залежно від відстані
- Інтеграція з OptimizationManager

---

### StructureGenerator.gd + WFCIntegrator.gd
**Призначення:** Генерація структур (будинки, вежі) через Wave Function Collapse  
**Батьківський клас:** Node  
**Статус:** ⚠️ Заготовка (вимкнено)

**Основні функції (StructureGenerator):**
- `generate_structure(gridmap, position, structure_type)` - генерація структури
- `place_building(gridmap, position, size)` - розміщення будинку
- `place_tower(gridmap, position, height)` - розміщення вежі

**Основні функції (WFCIntegrator):**
- `generate_with_wfc(gridmap, area, rules)` - генерація через WFC алгоритм
- `apply_wfc_rules(patterns)` - застосування правил WFC

**Особливості:**
- Wave Function Collapse для процедурної генерації
- Підтримка різних типів структур

---

### LargeStructureGenerator.gd
**Призначення:** Генерація великих структур (гори, каньйони, печери)  
**Батьківський клас:** Node  
**Статус:** ⚠️ Заготовка (вимкнено)

**Основні функції:**
- `generate_mountain(gridmap, center, radius, height)` - генерація гори
- `generate_canyon(gridmap, start, end, depth, width)` - генерація каньйону
- `generate_large_cave_system(gridmap, center, size)` - генерація системи печер

**Налаштування:**
- `enable_large_structures: bool` (false)
- `mountain_density: float` (0.1)
- `canyon_density: float` (0.05)
- `cave_density: float` (0.2)

**Параметри гір:**
- `mountain_min_height / mountain_max_height: int` (15 / 25)
- `mountain_radius_min / mountain_radius_max: int` (10 / 30)

**Параметри каньйонів:**
- `canyon_min_depth / canyon_max_depth: int` (8 / 15)
- `canyon_width_min / canyon_width_max: int` (3 / 8)

**Особливості:**
- Використовує окремі шуми для кожного типу структури
- Детермінована генерація через фіксовані seeds

---

### ErosionSimulator.gd
**Призначення:** Симуляція ерозії для природнішого рельєфу  
**Батьківський клас:** Node  
**Статус:** ⚠️ Заготовка (вимкнено)

**Основні функції:**
- `simulate_erosion_for_chunk(heightmap_data, chunk_pos, chunk_size)` - симуляція ерозії
- `_simulate_hydrological_step()` - крок гідрологічної ерозії
- `_simulate_thermal_step()` - крок теплової ерозії

**Налаштування:**
- `enable_erosion: bool` (false)
- `erosion_iterations: int` (50)
- `erosion_strength: float` (0.1)
- `sediment_capacity: float` (0.5)
- `evaporation_rate: float` (0.01)

**Теплова ерозія:**
- `enable_thermal_erosion: bool` (false)
- `thermal_iterations: int` (25)
- `talus_angle: float` (0.5) - кут природного укосу
- `thermal_strength: float` (0.1)

**Особливості:**
- Гідрологічна ерозія (вода змиває породу)
- Теплова ерозія (обвали на крутих схилах)
- Реалістичні ландшафти з природними формами

---

### HeightmapLoader.gd
**Призначення:** Завантаження та використання heightmap текстур  
**Батьківський клас:** Node  
**Статус:** ⚠️ Заготовка (вимкнено)

**Основні функції:**
- `load_heightmap(image_path)` - завантаження heightmap з файлу
- `generate_terrain_from_heightmap(gridmap, heightmap, scale)` - генерація з heightmap
- `sample_heightmap(x, z)` - семплінг висоти з heightmap

**Налаштування:**
- `heightmap_texture: Texture2D` - текстура heightmap
- `heightmap_scale: float` - масштаб висоти
- `heightmap_offset: Vector2` - зміщення

**Особливості:**
- Підтримка зовнішніх heightmap
- Інтеграція з процедурною генерацією
- Використовує SurfaceTool/ArrayMesh

---

### SplatMapManager.gd
**Призначення:** Splat mapping для текстурування терейну  
**Батьківський клас:** Node  
**Статус:** ⚠️ Заготовка (вимкнено)

**Основні функції:**
- `generate_splat_map(chunk_pos, heightmap)` - генерація splat map
- `apply_splat_textures(gridmap, splat_map)` - застосування текстур

**Налаштування:**
- `splat_textures: Array[Texture2D]` - текстури для splat mapping
- `blend_mode: String` - режим змішування

**Особливості:**
- Автоматичне текстурування залежно від висоти/крутості
- Плавні переходи між текстурами

---

### DetailLayerManager.gd
**Призначення:** Управління деталями (камені, трава, декор)  
**Батьківський клас:** Node  
**Статус:** ⚠️ Заготовка (вимкнено)

**Основні функції:**
- `generate_detail_layer(chunk_pos, gridmap)` - генерація деталей для чанка
- `remove_detail_layer(chunk_pos)` - видалення деталей

**Налаштування:**
- `detail_meshes: Array[Mesh]` - меші для деталей
- `detail_density: float` - щільність деталей

**Особливості:**
- Використовує MultiMesh для оптимізації
- Подібно до VegetationManager, але для дрібних деталей

---

### NativeOptimizer.gd
**Призначення:** Нативна оптимізація через GDExtension  
**Батьківський клас:** Node  
**Статус:** ⚠️ Заготовка (не реалізовано)

**Призначення:**
- Перенесення критичних операцій в C++ для швидкості
- Оптимізація mesh generation, noise sampling

**Особливості:**
- Вимагає GDExtension
- Потенційно 10-100x прискорення для критичних операцій

---

### BestPractices.gd
**Призначення:** Best practices для генерації терейну  
**Батьківський клас:** Node  

**Основні функції:**
- `implement_chunk_batching()` - batching чанків
- `implement_frustum_culling()` - frustum culling
- `implement_occlusion_culling()` - occlusion culling
- `implement_lod_system()` - LOD система
- `optimize_mesh_generation()` - оптимізація mesh
- `profile_performance(operation_name, start_time)` - профілювання

**Категорії best practices:**
- **Memory Management**: chunk pooling, memory limits
- **Rendering**: frustum culling, LOD, batching
- **Generation**: noise caching, pattern precomputation
- **Threading**: parallel generation, async loading

**Особливості:**
- Документує кращі практики
- Допоміжні функції для реалізації оптимізацій
- Профілювання продуктивності

---

## 🐛 DEBUG СИСТЕМИ

### ConsoleCommands.gd
**Призначення:** Команди для Panku Console  
**Батьківський клас:** Node  
**Взаємодія:** Player, TerrainGenerator, Panku Console

**Команди для гравця:**
- `teleport(x, y, z)` - телепортувати гравця
- `teleport_to_starting_area()` - телепорт на стартову зону
- `set_speed(multiplier)` - встановити швидкість (0.1-10.0)
- `toggle_flight()` - перемкнути польотний режим

**Команди для генерації:**
- `regenerate_chunk(x, z)` - перегенерувати чанк
- `regenerate_world()` - перегенерувати весь світ
- `get_performance_report()` - звіт про продуктивність
- `toggle_profiling()` - увімкнути/вимкнути профілювання

**Допоміжні команди:**
- `get_player_position()` - поточна позиція гравця
- `help()` - показати список команд

**Використання:**
Через Panku Console: `Debug.teleport(0, 10, 0)`, `Debug.toggle_flight()`

**Особливості:**
- Автоматично реєструється в Panku Console як "Debug"
- Знаходить гравця та TerrainGenerator при `_ready()`

---

## 🔄 ВЗАЄМОДІЯ СИСТЕМ

### Основні потоки даних:

**1. Генерація світу:**
```
TerrainGenerator → ChunkManager → ProceduralGeneration → GridMap
                ↓
          OptimizationManager (якщо увімкнено)
                ↓
          StartingAreaGenerator → Player
```

**2. Рух гравця:**
```
PlayerController → Camera → ChunkManager → генерація нових чанків
                                        → culling далеких чанків
```

**3. Build Mode:**
```
BuildController → GridMap.set_cell_item() → VoxelPhysics.check_stability()
                                         → GameEvents.block_placed/destroyed
```

**4. Інвентар та крафт:**
```
InventorySystem ← → CraftingSystem
       ↓                  ↓
  InventoryUI      CraftingMenu
       ↑                  ↑
 GameEvents.item_picked_up/crafted
```

**5. Потреби гравця:**
```
NeedsSystem → StatusEffects → PlayerStats
     ↓              ↓              ↓
CharacterMenu  (ефекти)     (пошкодження)
```

---

## 📊 СТАТУС РЕАЛІЗАЦІЇ

### ✅ Повністю реалізовано:
- **Autoload системи** (BlockRegistry, ResourceManager, InventorySystem, CraftingSystem, GameEvents)
- **UI системи** (main_menu, pause_menu, game_hud, inventory_ui, character_menu, crafting_menu, settings_menu, WorldGenerationSettings, loading_screen)
- **Player системи** (PlayerController, PlayerStats, CameraController, BuildController)
- **Ігрові системи** (NeedsSystem, StatusEffects)
- **Базова генерація** (TerrainGenerator, ProceduralGeneration, ChunkManager)
- **Біоми** з плавним blending
- **Воксельна фізика** обвалів (VoxelPhysics)
- **StartingAreaGenerator** з перевіркою колізій
- **PrecomputedPatterns** (патерни місцевості, структур, біомів)
- **SaveLoadManager** (збереження/завантаження чанків, автозбереження)
- **Утиліти** (WorldLogger, ObjectPool, Quadtree, types.gd)
- **Debug** (ConsoleCommands для Panku Console)

### ⚠️ Частково реалізовано (заготовки):
- **OptimizationManager** (працює, але гальмує)
- **Frustum Culling** (спрощена версія)
- **Chunk Boundary Fix** (не повністю працює)
- **Partial Mesh Updates** (структура є, не повністю працює)
- **VoxelNavigation** (заготовка, вимкнено)
- **Cave Generation** (ProceduralGeneration.enable_caves, вимкнено)
- **Large Structure Generator** (заготовка, вимкнено)
- **Erosion Simulator** (заготовка, вимкнено)
- **POIGenerator** (заготовка, вимкнено)
- **LODManager** (заготовка, вимкнено)
- **StructureGenerator + WFCIntegrator** (заготовка, вимкнено)
- **ThreadingManager** (заготовка, вимкнено, не інтегровано)
- **VegetationManager** (реалізовано, вимкнено)
- **HeightmapLoader** (заготовка, вимкнено)
- **SplatMapManager** (заготовка, вимкнено)
- **DetailLayerManager** (заготовка, вимкнено)
- **Quadtree** (реалізовано, не інтегровано в ChunkManager)

### ❌ Не реалізовано:
- **Greedy Meshing** (закоментовано в OptimizationManager)
- **Indexed Buffers / GPU Optimization**
- **Physics Optimization** (окрема фізика для активних чанків)
- **Memory Pooling** для блоків (ObjectPool реалізовано, але не для блоків)
- **Повна реалізація Partial Mesh Updates** (не оновлює грані сусідніх блоків)
- **NativeOptimizer** (заготовка, вимагає GDExtension)

---

## 🎯 КРИТИЧНІ ПРОБЛЕМИ

### 1. Mesh Optimization гальмує
**Проблема:** `OptimizationManager.optimize_chunk_mesh()` працює погано  
**Причина:** `collect_chunk_data()` + `_rebuild_chunk_with_optimized_mesh()` - подвійна робота  
**Рішення:** Виправити `_has_neighbor_block_in_adjacent_chunk()`, оптимізувати збір даних

### 2. Chunk Boundaries - видимі стики
**Проблема:** Користувач бачить квадратну сітку чанків  
**Причина:** `_get_height_with_boundary_smoothing()` не повністю працює  
**Рішення:** Покращити smoothing, перевірити `_gather_neighbor_chunk_data()`

### 3. Довге завантаження (~5 хв)
**Проблема:** Початкове завантаження світу займає ~5 хв  
**Причина:** Оптимізація гальмує, генерація всіх чанків одразу  
**Рішення:** Вимкнути `use_optimization`, використати async генерацію

### 4. Фризи при русі
**Проблема:** Фризи при швидкому русі до межі чанків  
**Причина:** Генерація чанків блокує основний потік  
**Рішення:** Використати `ThreadingManager`, обмежити генерацію на кадр

---

## 📝 ПРИМІТКИ

### Конвенції коду:
- Назви файлів: PascalCase для класів (TerrainGenerator.gd), snake_case для утиліт (types.gd)
- Змінні: snake_case
- Константи: UPPER_SNAKE_CASE
- Приватні методи: `_prefix`
- Export параметри: `@export` для налаштувань

### Патерни:
- **Singleton**: Autoload системи (BlockRegistry, InventorySystem, GameEvents)
- **Observer**: Сигнали для комунікації (inventory_changed, stat_changed)
- **Module**: TerrainGenerator з модульною архітектурою
- **Flyweight**: BlockRegistry - переви використання блоків
- **Object Pool**: ObjectPool для повторного використання об'єктів
- **Spatial Partitioning**: Quadtree для оптимізації пошуку

### Оптимізація:
- Chunk-based генерація
- Culling далеких чанків
- Frustum culling
- Preloading buffer
- Object pooling (заготовка)
- Memory pooling (заготовка)

---

**Кінець документації**

*Для детальної інформації про плани розвитку див. ROADMAP.md*  
*Для відслідковування змін див. CATCHUP.md*

