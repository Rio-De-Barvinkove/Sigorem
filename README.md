# Sigorem - 2.5D Survival Game

## 🎮 Про проект
2.5D піксельна гра-виживалка в Godot 4.5, натхненна Vintage Story та TerraFirmaCraft.

### Референси
- **Візуальний стиль**: 2.5D діорамний вигляд (як Octopath Traveler)
- **Фізика вокселів**: Реалістичні обвали (як Lay of Land)
- **Геймплей**: Хардкорне виживання з реалістичними механіками

## 🚀 Поточний статус

### ✅ Реалізовано (Phase 0)
- [x] Базове налаштування проекту
- [x] Flat/procedural terrain toggle
- [x] Input actions (build, inventory, pause)
- [x] Autoload системи
- [x] 2.5D камера з діорамним ефектом (45°)
- [x] Pixel-perfect рендеринг
- [x] Воксельна фізика обвалів
- [x] Поворот камери (Q/E)
- [x] Зум камери (колесо миші)

### 🔧 В роботі (Phase 1 - MVP)
- [ ] Імпорт асетів TheLazyStone
- [ ] Y-sort для правильної глибини
- [ ] Система збору ресурсів
- [ ] UI інвентаря
- [ ] Базовий крафтинг

### 📝 Заплановано
- Система потреб (голод, спрага, сон)
- Сезони та погода
- Температура та вологість
- Процедурна генерація
- НПС з власним життям
- Шари ґрунту

## 🛠️ Технічні особливості

### Воксельна фізика
- Реалістичні обвали блоків
- Перевірка підтримки структур
- Ланцюгові руйнування
- Фізичні блоки що падають

### 2.5D камера
- Діорамний кут 45°
- Плавне слідування за гравцем
- 8 напрямків повороту (кожні 45°)
- Динамічний зум

### Terrain Generation
- Процедурна генерація з шумом
- Flat режим для тестування
- Шари: камінь → земля → трава

## 📦 Асети
Тимчасово використовуємо:
- [TheLazyStone Post-Apocalypse Pack](https://thelazystone.itch.io/post-apocalypse-pixel-art-asset-pack)
- 3300+ спрайтів
- 16x16 піксель арт

## 🚧 Відомі проблеми
- WorldObjects spawner ще не підключений до сцени
- Рецепти крафтингу не створені
- UI меню потребують підключення

## 💡 Унікальні механіки (заплановано)
- Кожен сезон "душить" по-своєму
- Зміна кута камери залежно від оточення
- Інвентар з вагою та об'ємом
- НПС з економікою та настроєм
- Система репутації (як Kingdom Come)
- Обвали в шахтах

## 🔗 Посилання
- [Дизайн документ](docs/survival_design.md)
- [План розробки](god.plan.md)

---
# 🚀 Швидкий старт Sigorem

## 3 кроки до запуску:

### 1️⃣ Створіть MeshLibrary (блоки для світу)
`1. Відкрийте create_meshlib.gd`
`2. File → Run (Ctrl+Shift+X)`
`3. Подивіться Output - має бути "✅ успішно збережено"`

### 2️⃣ Перезавантажте World сцену
`1. Відкрийте scenes/world.tscn`
`2. Scene → Reload Saved Scene`
`3. Виберіть GridMap → має з'явитися секція "Terrain Generation"`

### 3️⃣ Запустіть гру
`1. Натисніть F6 (Current Scene) або F5 (Project)`
`2. Має з'явитися 3D світ з блоками`

---

## ⌨️ Керування:

| Клавіша | Дія |
|---------|-----|
| **WASD** | Рух |
| **Q** | Камера ліворуч |
| **R** | Камера праворуч |
| **Колесо миші** | Зум |
| **B** | Build Mode |
| **ЛКМ** | Поставити блок (в Build Mode) |
| **ПКМ** | Видалити блок (в Build Mode) |
| **I** | Інвентар |
| **C** | Меню персонажа |
| **ESC** | Пауза |

---

## ⚠️ Якщо щось не працює:

### GridMap порожній
→ Запустіть `create_meshlib.gd` (Крок 1)

### Не бачу "Terrain Generation"
→ Scene → Reload Saved Scene

### Player падає
→ У Player змініть Position Y на **6**

### Build Mode не працює
→ Натисніть **B** двічі (увімкнути/вимкнути)

---

## 🎯 Що тестувати:

1. ✅ **Рух** - WASD працює
2. ✅ **Камера** - Q/R обертає, колесо зумить
3. ✅ **Build Mode** - B показує примарний блок
4. ✅ **Будівництво** - ЛКМ ставить, ПКМ ламає
5. ✅ **ФІЗИКА ОБВАЛІВ** - побудуйте башту, видаліть низ, блоки мають ПАДАТИ!

---
# 🚀 Покроковий гайд налаштування та тестування Sigorem

## Крок 1: Відкриття проекту в Godot

1. **Запустіть Godot 4.5**
2. **Імпортуйте проект:**
   - Натисніть "Import"
   - Оберіть файл `project.godot` в папці `D:\Az_projects\SergoSarofan\`
   - Натисніть "Import & Edit"
3. **Зачекайте** поки Godot імпортує всі ресурси (перший раз може зайняти час)

---

## Крок 2: Перевірка базових налаштувань

### 2.1 Перевірка Autoload систем
1. Перейдіть **Project → Project Settings → Autoload**
2. Переконайтеся що є такі системи:
   - ✅ `GameEvents` → `res://scripts/systems/game_events.gd`
   - ✅ `TextureManager` → `res://scripts/world/texture_manager.gd`
   - ✅ `InventorySystem` → `res://scripts/inventory/inventory_system.gd`
   - ✅ `CraftingSystem` → `scripts/systems/crafting_system.gd`
   - ✅ `VoxelPhysics` → `res://scripts/world/voxel_physics.gd`

### 2.2 Перевірка Input Map
1. **Project → Project Settings → Input Map**
2. Переконайтеся що налаштовані клавіші:
   - **ui_left/right/up/down** - стрілки або WASD (для руху)
   - **build_mode** - B
   - **toggle_inventory** - I
   - **toggle_pause** - ESC
   - **character_menu** - C
   - **crafting_menu** - K
   - **interact** - E
   - **camera_rotate_left** - Q
   - **camera_rotate_right** - E (можливо конфлікт, змініть на Right Shift якщо треба)

**❗ ВАЖЛИВО:** Якщо `camera_rotate_right` = E, змініть на іншу клавішу (наприклад Right Shift або R), бо E вже зайнято під `interact`

---

## Крок 3: Налаштування World Scene

### 3.1 Відкрийте World сцену
1. В **FileSystem** знайдіть `scenes/world.tscn`
2. **Двічі клікніть** щоб відкрити

### 3.2 Перевірка структури сцени
У Scene панелі має бути:
`World (Node3D)`
`├── GridMap`
`├── Player (CharacterBody3D)`
`├── Camera3D`
`└── UI (CanvasLayer)`
`    ├── InventoryUI`
`    ├── PauseMenu`
`    └── CharacterMenu`

### 3.3 Налаштування GridMap (Terrain)
1. **Виберіть `GridMap`** в сцені
2. В **Inspector** знайдіть налаштування:
   - **Mesh Library**: має бути `res://resources/terrain_meshlib.tres`
   - **Script**: `res://scripts/world/tile_map.gd`

3. **Налаштування генерації:**
   - **Use Procedural**: ☑️ (увімкніть для рельєфу) або ☐ (вимкніть для плоскої карти)
   - **Flat Height**: 5 (висота плоскої карти)
   - **Noise Amplitude**: 5 (амплітуда рельєфу)
   - **Base Height**: 5 (базова висота)
   - **Grass Block Id**: 0
   - **Dirt Block Id**: 1
   - **Stone Block Id**: 2

**💡 Порада:** Для першого тесту вимкніть `Use Procedural` щоб отримати плоску карту

### 3.4 Налаштування Camera3D
1. **Виберіть `Camera3D`**
2. В **Inspector** перевірте:
   - **Target**: має вказувати на `../Player`
   - **Offset**: (10, 15, 10)
   - **Smooth Speed**: 5.0
   
3. **2.5D Settings:**
   - **Diorama Angle**: 45.0
   - **Enable Tilt Shift**: true
   - **Min Zoom**: 5.0
   - **Max Zoom**: 30.0
   - **Zoom Speed**: 1.0
   - **Rotation Enabled**: true

### 3.5 Налаштування Player
1. **Виберіть `Player`**
2. **Transform** → **Position**: змініть Y на 6 (щоб був над землею)
3. Перевірте що є **BuildMode** child node

---

## Крок 4: Створення MeshLibrary (КРИТИЧНО!)

**❗ Без цього крок нічого не працюватиме!**

### АВТОМАТИЧНИЙ СПОСІБ (РЕКОМЕНДОВАНО) ⚡

1. **Знайдіть файл** `create_meshlib.gd` в корені проекту (FileSystem)

2. **Відкрийте його** подвійним кліком

3. **Запустіть скрипт:**
   - В меню зверху: **File → Run** 
   - АБО натисніть **Ctrl+Shift+X**

4. **Подивіться Output** (внизу екрану):
   `=== Створюємо MeshLibrary ===`
   `✓ Grass block створено (ID: 0)`
   `✓ Dirt block створено (ID: 1)`
   `✓ Stone block створено (ID: 2)`
   `✅ MeshLibrary успішно збережено в: res://resources/terrain_meshlib.tres`

5. **Готово!** MeshLibrary створений автоматично з колізіями!

### ЯКЩО АВТОМАТИЧНИЙ СПОСІБ НЕ СПРАЦЮВАВ:

Дивіться `docs/MESH_LIBRARY_FIX.md` для ручного створення

---

## Крок 5: Фінальна перевірка та виправлення помилок

### 5.1 Перевірка VoxelPhysics
1. Відкрийте `scripts/world/voxel_physics.gd`
2. **Знайдіть рядок 22:** `grid_map = get_node("/root/World/GridMap")`
3. **Закоментуйте connect**, змініть на:
`func _ready():`
`	await get_tree().process_frame  # Зачекаємо один фрейм`
`	grid_map = get_node("/root/World/GridMap")`
`	# Не підключаємо сигнал, бо його немає в GridMap`

### 5.2 Виправлення camera_rotate_right
1. **Project → Project Settings → Input Map**
2. Знайдіть **camera_rotate_right**
3. Видаліть E (конфлікт з interact)
4. Додайте **Right Shift** або **R**

---

## Крок 6: ПЕРШИЙ ЗАПУСК!

### 6.1 Запуск гри
1. **Натисніть F5** або **Play кнопку** ▶️
2. Якщо попросить вибрати Main Scene → оберіть `scenes/main_menu.tscn`
3. Натисніть **Play**

### 6.2 Що має відбутися:
- ✅ Відкриється Main Menu
- ✅ Можна натиснути кнопку (якщо є UI)

### 6.3 Якщо помилки:
Подивіться **Output/Debugger** панель знизу. Частіше за все:
- **"res://resources/terrain_meshlib.tres is not a valid MeshLibrary"** → повторіть Крок 4
- **"Can't find node: /root/World/GridMap"** → нормально, ігноруйте поки не в World сцені

---

## Крок 7: Тестування World Scene

### 7.1 Запуск World напряму
1. **Відкрийте** `scenes/world.tscn`
2. **Натисніть F6** (Play Current Scene)

### 7.2 Що має з'явитися:
- ✅ **Terrain** - плоска або з рельєфом карта з блоків
- ✅ **Player** - білий куб/спрайт
- ✅ **Camera** - вид під кутом 45° зверху

### 7.3 Тестування руху
- **WASD або стрілки** → гравець має рухатися
- **Q** → камера повертається ліворуч
- **R/Right Shift** → камера повертається праворуч
- **Колесо миші вгору/вниз** → зум камери

---

## Крок 8: Тестування Build Mode (ГОЛОВНЕ!)

### 8.1 Активація Build Mode
1. **Натисніть B** → має увімкнутися режим будівництва
2. **На мишці має з'явитися напівпрозорий блок-примара** (ghost block)

### 8.2 Тестування розміщення блоків
1. **ЛКМ (ліва кнопка миші)** → ставить блок
2. Спробуйте поставити **5-10 блоків** вертикально (башту)

### 8.3 Тестування ВОКСЕЛЬНОЇ ФІЗИКИ! 🔥
1. **ПКМ (права кнопка миші)** → видаляє блок
2. **ВИДАЛІТЬ НИЖНІЙ БЛОК** з вашої башти
3. **ЩО МАЄ СТАТИСЯ:**
   - ✅ Блоки зверху **почнуть падати**
   - ✅ Вони **обертаються** в повітрі
   - ✅ **Ланцюгова реакція** - якщо видалити середину, обидві сторони впадуть

**💡 Це і є воксельна фізика як у Lay of Land!**

### 8.4 Якщо блоки не падають:
1. Перевірте **Output** - чи немає помилок про VoxelPhysics
2. Переконайтеся що **StaticBody3D** доданий до блоків (Крок 4.1)
3. У `scripts/player/build_mode.gd` має бути виклик `VoxelPhysics.check_stability_around()`

---

## Крок 9: Тестування UI

### 9.1 Інвентар
1. **Натисніть I** → має відкритися інвентар
2. Якщо нічого - нормально, UI ще не підключений повністю

### 9.2 Пауза
1. **Натисніть ESC** → має з'явитися меню паузи
2. Гра має зупинитися

### 9.3 Character Menu
1. **Натисніть C** → має відкритися меню персонажа

---

## Крок 10: Налаштування для кращої картинки

### 10.1 Pixel Perfect налаштування
1. **Project → Project Settings → Display**
2. Перевірте:
   `Window/Size/Viewport Width: 1280`
   `Window/Size/Viewport Height: 720`
   `Window/Stretch/Mode: viewport`

3. **Rendering → Textures:**
   `Canvas Textures/Default Texture Filter: Nearest`

### 10.2 Освітлення (опціонально)
1. В World сцені додайте **DirectionalLight3D**
2. **Rotation**: (-45, -45, 0) для діорамного освітлення
3. **Energy**: 0.8
4. **Shadow Enabled**: ✅

---

## 🎯 Чеклист успішного тестування

- [ ] Проект відкривається без помилок
- [ ] Main Menu запускається
- [ ] World scene генерує terrain
- [ ] Гравець рухається (WASD)
- [ ] Камера зумиться (колесо) та обертається (Q/R)
- [ ] Build Mode (B) показує ghost block
- [ ] ЛКМ ставить блоки
- [ ] ПКМ видаляє блоки
- [ ] **БЛОКИ ПАДАЮТЬ коли втрачають підтримку** ⭐
- [ ] Можна відкрити UI меню (I, ESC, C)

---

## ❗ Типові проблеми та рішення

### Проблема: "MeshLibrary is empty"
**Рішення:** Повторіть Крок 4 - створення MeshLibrary

### Проблема: Player падає крізь землю
**Рішення:** 
- Перевірте що у блоках є StaticBody3D з CollisionShape3D
- Змініть Y позицію Player на 6

### Проблема: Камера не слідує за гравцем
**Рішення:**
- У Camera3D → Target має бути `../Player` (NodePath)

### Проблема: Блоки не падають
**Рішення:**
- Перевірте що VoxelPhysics в Autoload
- У Output подивіться чи немає помилок
- Переконайтеся що `build_mode.gd` викликає `VoxelPhysics.check_stability_around()`

### Проблема: Build Mode не активується
**Рішення:**
- Перевірте Input Map → build_mode = B
- Перевірте що Player має child node BuildMode

---

## 📝 Наступні кроки після успішного тесту

1. **Імпорт TheLazyStone текстур**
2. **Створення gatherable objects** (дерева, камені)
3. **Підключення UI до систем**
4. **Додавання рецептів крафтингу**

---
# Швидке створення MeshLibrary в Godot 4

## Проблема
У Godot 4 немає "Scene → Convert To → MeshLibrary"

## Рішення 1: Через Inspector (НАЙПРОСТІШЕ)

1. **Створіть тимчасову сцену:**
   - Scene → New Scene → 3D Scene
   - **НЕ ЗБЕРІГАЙТЕ** її зараз

2. **Додайте ваші блоки як дочірні ноди:**
   - З FileSystem **перетягніть** `scenes/blocks/grass_block.tscn` в сцену
   - Перетягніть `dirt_block.tscn`
   - Перетягніть `stone_block.tscn`

3. **Виберіть ROOT node** (Node3D)

4. **Inspector → Правий верхній кут → три крапки ⋮**

5. **"Save Branch as Scene"** → збережіть як `temp_blocks.tscn`

6. **Тепер створюємо MeshLibrary:**
   - У FileSystem знайдіть `resources/terrain_meshlib.tres`
   - **Видаліть його** (правий клік → Delete)
   - Правий клік на папці `resources/`
   - **New Resource...**
   - Шукайте "MeshLibrary"
   - Створіть і збережіть як `terrain_meshlib.tres`

7. **Відкрийте** `terrain_meshlib.tres` (подвійний клік)

8. **Inspector → MeshLibrary → Items:**
   - Натисніть **"+"** тричі (для 3 блоків)
   
9. **Для кожного Item:**
   - **Item 0 (Grass):**
     - Mesh: перетягніть MeshInstance3D з grass_block
     - Shape: додайте BoxShape3D
     
   - **Item 1 (Dirt):**
     - То саме для dirt
     
   - **Item 2 (Stone):**
     - То саме для stone

## Рішення 2: Спрощений варіант (БЕЗ окремих блоків)

Якщо у вас ще немає готових сцен блоків - створимо все швидко:

### Крок 1: Створіть один тестовий блок

1. **Scene → New Scene → 3D Scene**
2. **Перейменуйте Node3D** → "GrassBlock"
3. **Додайте MeshInstance3D:**
   - Add Child Node → MeshInstance3D
   - Inspector → Mesh → **New BoxMesh**
   - BoxMesh → Size: (1, 1, 1)
   - Material → **New StandardMaterial3D**
   - Albedo → Color: **зелений #4CAF50**

4. **Збережіть** як `scenes/blocks/grass_block.tscn`

5. **Дублюйте для інших:**
   - FileSystem → правий клік на grass_block.tscn
   - **Duplicate**
   - Назвіть `dirt_block.tscn`
   - Відкрийте, змініть колір на коричневий #8B4513
   - Збережіть
   - Повторіть для `stone_block.tscn` (сірий #808080)

### Крок 2: Створіть MeshLibrary вручну

1. **FileSystem → resources/ → правий клік → New Resource**
2. Шукайте **"MeshLibrary"**
3. Створіть, збережіть як `terrain_meshlib.tres`
4. **Подвійний клік** щоб відкрити
5. **Додайте Items вручну** (дивись вище)

## Рішення 3: Через скрипт (АВТОМАТИЧНО)

Створіть тимчасовий скрипт який все зробить:
`@tool`
`extends EditorScript`

`func _run():`
`	var mesh_lib = MeshLibrary.new()`
`	`
`	# Grass block (ID 0)`
`	var grass_mesh = BoxMesh.new()`
`	grass_mesh.size = Vector3.ONE`
`	var grass_mat = StandardMaterial3D.new()`
`	grass_mat.albedo_color = Color(0.3, 0.8, 0.3)  # Зелений`
`	grass_mesh.surface_set_material(0, grass_mat)`
`	mesh_lib.create_item(0)`
`	mesh_lib.set_item_mesh(0, grass_mesh)`
`	`
`	# Dirt block (ID 1)`
`	var dirt_mesh = BoxMesh.new()`
`	dirt_mesh.size = Vector3.ONE`
`	var dirt_mat = StandardMaterial3D.new()`
`	dirt_mat.albedo_color = Color(0.55, 0.27, 0.07)  # Коричневий`
`	dirt_mesh.surface_set_material(0, dirt_mat)`
`	mesh_lib.create_item(1)`
`	mesh_lib.set_item_mesh(1, dirt_mesh)`
`	`
`	# Stone block (ID 2)`
`	var stone_mesh = BoxMesh.new()`
`	stone_mesh.size = Vector3.ONE`
`	var stone_mat = StandardMaterial3D.new()`
`	stone_mat.albedo_color = Color(0.5, 0.5, 0.5)  # Сірий`
`	stone_mat.surface_set_material(0, stone_mat)`
`	mesh_lib.create_item(2)`
`	mesh_lib.set_item_mesh(2, stone_mesh)`
`	`
`	# Зберігаємо`
`	ResourceSaver.save(mesh_lib, "res://resources/terrain_meshlib.tres")`
`	print("MeshLibrary створено!")`

**Як використати:**
1. File → New Script
2. Вставте код вище
3. Збережіть як `create_meshlib.gd`
4. **File → Run** (Ctrl+Shift+X)
5. Готово! MeshLibrary створений автоматично

## Перевірка

Після створення MeshLibrary:
1. Відкрийте `scenes/world.tscn`
2. Виберіть GridMap
3. Inspector → Mesh Library має показувати `terrain_meshlib.tres`
4. Внизу екрану має з'явитися **GridMap panel** з блоками #0, #1, #2

Якщо бачите блоки - все працює! 🎉

---

## ❓ FAQ - Швидкі відповіді

### Q: Де знайти налаштування "Use Procedural" в GridMap?
**A:** 
1. Відкрийте `scenes/world.tscn`
2. Виберіть GridMap
3. **Закрийте та знову відкрийте сцену** (Scene → Reload Saved Scene)
4. Inspector → прокрутіть вниз → має з'явитися секція "Terrain Generation"

### Q: Не бачу BuildMode у Player
**A:**
1. Виберіть Player в Scene tree
2. **Натисніть стрілочку ►** ліворуч від Player щоб розгорнути
3. Якщо не розгортається - Scene → Reload Saved Scene

### Q: Немає кнопки "Scene → Convert To → MeshLibrary"
**A:** В Godot 4 цієї кнопки немає! Використайте:
- **Автоматичний скрипт** `create_meshlib.gd` (File → Run або Ctrl+Shift+X)
- АБО дивіться `docs/MESH_LIBRARY_FIX.md` для ручного створення

### Q: Скрипт create_meshlib.gd не запускається
**A:**
1. Переконайтеся що файл відкритий в редакторі
2. **Зверху має бути @tool** на першому рядку
3. File → Run (НЕ Play Scene!)
4. Подивіться Output внизу - там має бути результат

### Q: GridMap порожній після запуску
**A:**
1. Перевірте чи MeshLibrary підключений (GridMap → Inspector → Mesh Library)
2. Запустіть create_meshlib.gd якщо ще не робили
3. Перевірте Output на помилки при старті

### Q: Player падає крізь землю
**A:**
1. У world.tscn виберіть Player
2. Transform → Position → змініть Y на **6** або **10**
3. Переконайтеся що блоки мають колізію (create_meshlib.gd додає автоматично)

### Q: Камера не рухається/не слідує
**A:**
1. Camera3D → Inspector → Target має бути `../Player`
2. Якщо пусто - клікніть на поле та виберіть Player

### Q: Build Mode не активується на B
**A:**
1. Project → Project Settings → Input Map
2. Знайдіть `build_mode` 
3. Якщо немає - додайте і прив'яжіть до клавіші B
